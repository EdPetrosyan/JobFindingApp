@model IEnumerable<JobFindingModels.DTOs.GetJobsForListingDto>

@{
    ViewData["Title"] = "Jobs";
}

<h1>Jobs</h1>

<form asp-controller="Jobs" asp-action="SearchJob">
    <p>
        Search Job: <input type="text" name="input" />
        <input type="submit" value="Search" />
    </p>
</form>

<form asp-controller="Jobs" asp-action="GetFilters">
    <p>
        <input type="submit" value="Filters" />
    </p>
</form>

<p>
    <a asp-area="" asp-controller="Jobs" asp-action="CreateJob">Create new Job</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CompanyName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsBookmarked)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CompanyName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Location)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IsBookmarked)
                    <a asp-action="MarkAsBookmarked" asp-route-id="@item.Id">Book/Unbook</a>

                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.CreatedDate)
                </td>

                <td>
                   
                    <a asp-action="GetJob" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="DeleteJob" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
